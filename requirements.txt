# Embeddings
sentence-transformers==2.2.2  # Mistral embedding model, runs locally

# Vector Database
weaviate-client==4.1.1  # Self-hosted vector DB with metadata filtering

# Data handling
python-dotenv==1.0.0  # Environment variables
pydantic==2.5.0  # Data validation and schema definition

# LLM integration (for later)
langchain==0.1.0  # Orchestration framework
openai==1.3.0  # For GPT-based synthesis

# Utilities
numpy==1.24.0
pandas==2.0.0
